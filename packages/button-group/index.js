import IsButtonGroup from '../button/src/button-group';

/* istanbul ignore next */
IsButtonGroup.install = function(Vue) {
  Vue.component(IsButtonGroup.name, IsButtonGroup);
};

export default IsButtonGroup;
