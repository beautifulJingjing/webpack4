import IsOptionGroup from '../select/src/option-group';

/* istanbul ignore next */
IsOptionGroup.install = function(Vue) {
  Vue.component(IsOptionGroup.name, IsOptionGroup);
};

export default IsOptionGroup;
